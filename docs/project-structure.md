# Project Structure and Repository Organization

## Directory Structure

### Key Directories
- `message/` - Contains message files and their associated metadata. This directory is tracked in the repository.

### Important Files
- `chat.html` - Must be preserved
- `index.html` - Must be preserved

## Git Tracking
All directories and files are tracked in git by default unless explicitly ignored in `.gitignore`.
